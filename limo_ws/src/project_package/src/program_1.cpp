#include <sstream>
#include "ros/ros.h"
#include "std_msgs/String.h"
#include <geometry_msgs/Twist.h>
#include "../include/stokes_motion_functions.h"



int main(int argc, char **argv)
{
    	// The ROS node is initialized
    	ros::init(argc, argv, "program_1");


	//Drive(float velocity(percent), float time(seconds));
	//Turn(float velocity(percent), float time(seconds));
	//Strafe(float velocity(percent), float time(seconds));

	//Type code in below using the functions above







	///////////////////////////////////////////////

	return 0;

}

